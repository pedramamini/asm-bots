        mov     ax,
        mov     ax 5
        mov     ax, [bx
        mov     ax, (1 + 2
        mov     ax, 1 +
        mov     ax, * 3
        mov     ax, ]
        db      1,
        db
5       nop
start:: nop
        rep
        rep     rep movsb
        times   3
        times
        mov     ax, strict 5
        mov     ax, word
        jmp     short
        org     0, 1
        align   4, db 0
        resb    2, 3
        mov     ax, 1 2
