        mov     ax, [bx+bp]
        mov     ax, [si+di]
        mov     ax, [bx*2]
        mov     ax, [-bx]
        mov     ax, [bx - si]
        mov     ax, [ax]
        mov     ax, [sp+2]
        mov     al, [bx+si+di]
        mov     ax, [4 - (si)]
        mov     ax, [(bx << 1) + 2]
        lea     si, [bl]
        mov     ax, [bp+bp]
