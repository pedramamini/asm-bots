---
type: note
title: UX phone fixes (F6 to F9)
created: 2026-09-25
tags: [ux, phone]
---

# UX phone fixes (F6 to F9)

Verified on production: CI run 36182048200 and Deploy run 36184728753 green for `72d7dbb`. `wrangler deployments status` shows Worker version `756b57d4` at 100% (the previous deploy was `c6519962`). `/api/version` reports `2026.09.25a` (the stamp is a date, so it does not change on each deploy of the day). Screenshots: `bun run scripts/ux-shots.ts https://asmbots.io /tmp/ux-final`.

| Finding | What changed | Commit | Production check |
| --- | --- | --- | --- |
| F6 editor on a phone | New `phone` preset (source over problems, everything else hidden), applied as a session layout under 768 px; the stored layout is not changed | `6d2c5ba` | `editor-mobile.png`: one column, source over problems |
| F7 panel header actions | The kit `Panel` header wraps under `md`; the actions drop to a second line, right-aligned | `d9aac5b` | `hill-main-mobile.png`: `share` and `sign in to submit` fully visible |
| F8 arena replay map | Under `lg` the battle box is the map's height (container query); under `md` the HUD chips go to a second row, fps and zoom chips hidden | `a2199d6` | `arena-replay-mobile.png`: map fills the width, no chip overlap |
| F9 home hero | From `xl` the hero is a three-column grid: name card, square map, legend; black only behind the map | `d01544e`, `72d7dbb` | `home-desktop.png`: no empty black band |

## Left open

Seen in the production shots, outside the scope of F6 to F9:

- **Editor toolbar at 390 px**: the toolbar overflows to the right (`LINT` is clipped), and the `NEW` button is partly under the `>` button. The source panel is short (about 150 px) with empty page below it, its title row is clipped at the top, and the `NEW BOT` template row sits over the source.
- **Arena replay at 390 px**: the transport row overflows (`ROUND 1/…` is clipped), and the bots table's name column is too narrow to show the names.
